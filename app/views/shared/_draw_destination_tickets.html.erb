<%= form_tag(game_player_destination_tickets_assign_path({ :game_id => @game.id, :player_id => @game.turn_player.id }), method: "patch") do |f| %>
  <%= submit_tag(t(:'DRAW_DESTINATION_TICKETS'), class: "btn btn-default btn-sm") %>
<% end %>
